React uses the mono-repo structure to manage various packages. The source code mainly includes the following parts:
- Fixtures: test case
- packages: the main part, including scheduler, reconciler, etc
- scripts: react build related
